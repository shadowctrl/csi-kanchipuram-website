import React from "react";
import styled from "styled-components";

const main = () => {
  return (
    <global>
      <imageslide>main</imageslide>
    </global>
  );
};

export default main;

const global = styled.div``;
